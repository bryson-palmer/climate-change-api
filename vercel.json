{
  "version": 2,
  "functions": {
    "api/*.js": {
      "memory": 1024,
      "maxDuration": 10
    }
  },
  "buildCommand": "npx playwright install-deps && npx playwright install chromium",
  "env": {
    "PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD": "0"
  },
  "regions": ["pdx1"],
  "engines": {
    "node": "18.x"
  }
}