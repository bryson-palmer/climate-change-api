{
  "version": 2,
  "functions": {
    "api/*.js": {
      "memory": 1024,
      "maxDuration": 20
    }
  },
  "buildCommand": "npx playwright install chromium",
  "installCommand": "npm install && npx playwright install-deps",
  "env": {
    "PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD": "0"
  },
  "regions": ["pdx1"]
}